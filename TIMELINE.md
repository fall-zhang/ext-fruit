## 当前内容

> 能运行的内容，先运行，当需要添加新功能的时候，进行重构

### stage 1 

应用可用，可以作为一个查词应用进行使用

- [x] 将 redux 转换为 zustand
- [x] 更新 database
- [x] 更新 i18n
- [x] 将 _helpers 中的内容全部迁移到对应的模块内
- [x] 使用 css 变量替代 scss 的变量
- [x] 使用 @tanstack/react-router 实现路由
- [x] 搜索 useSelector，用 zustand 的 useDictStore 替换
- [x] 使应用可以打包
- [x] api search 查询应该和 globalConfig 进行解耦
- [x] 移除 process.env
- [x] 走通一个翻译，以及对应的视图
- [x] 自动 dark mode
- [ ] 需要移除的 package
  - [x] 用 zustand 的 useDictStore 替换 useDispatch
  - [ ] 使用 es-toolkit 替换 lodash
  - [x] react-redux 原因，使用 zustand 代替，更少的模板代码
  - [X] observable-hooks 原因，作者自己写的库，不具有普遍性，和 rxjs 深度绑定，额外增加软件复杂度
  - [x] react-transition-group 原因，之后会使用新的动画，motion 代替
- [ ] 配置功能
  - [x] 配置保存到本地
  - [x] 并且从本地读取配置的功能
  - [x] 更改配置后，本地配置更新
  - [ ] 在配置支持的翻译时，勾选启用或者禁用，如果需要认证（需要登录，此时选项默认禁用），需要先认证才能勾选
- [x] 可以打包为本地应用使用
- [x] 查词功能
- [x] 修复所有 typescript 类型问题
- [ ] 拥有一个菜单，可以进入其它视图
  - [ ] 配置视图 dark 模式
  - [ ] 配置视图
  - [ ] 生词本视图
- [x] 比起前进和后退，历史功能比该功能更有用，可以直接看到对应的单词的搜索记录
- [x] 当前查询内容是一个单词，并且为英文的时候，给出一系列建议，如果为多行，关闭建议
- [ ] 每个词典的返回结果可以单独收藏，保存（所有词典就要统一翻译返回结果，不然非常难做）
- [ ] 在点击图标后，自动获取剪切板的内容，然后查询
- [ ] 想做拖拽对词典列表排序，要先实现配置能保存在本地，要做配置页面更新
- [ ] 验证所有 API 以及对应的视图
- [ ] 未认证 API，没翻译结果的 API，不会展示翻译结果
- [ ] 调用大模型生成当前的翻译
- [ ] core 中需要的功能
  - [ ] 单词查询服务封装
  - [ ] tauri 相关接口封装
  - [ ] 插件服务，可以载入自定义翻译插件
- [ ] 简化 core 模块，多余模块从 core 中移出
  - [ ] 词典保存功能，当前是保存到浏览器的 indexDB 中，流量器插件只能保存在这里，其它平台可以保存到本地，提供一个默认模块
  - [ ] 单词同步功能，非核心功能
  - [ ] profile 一整套配置的保存，不许要在核心实现
  - [ ] 配置功能，需要从 core 中移出，core 并不提供这些功能，只需要读取配置即可
  - [ ] 更多的词典配置
- [ ] 当点击托盘图标的时候，展示翻译 panel，在系统右下角，点击即可折叠
- [ ] 当前有一个配置页面，如果可以作为组件使用，可以通过操作生成对应的配置
- [ ] 既然配置被移动到了 core 之外，那么所有用到配置的地方，也都要移动到 core 之外
- [ ] 监听当前页面选中的文本，然后在右上角给弹出一个框的组件
- [ ] 一个弹窗组件
- [ ] weak up position，应用唤醒位置配置
  - [ ] 屏幕的四个方向右下角，鼠标右下角
- [ ] 历史模块旁边添加生词本功能
  - [ ] 历史模块中，可以点击添加到生词本
- [ ] 一个翻译组件
- [ ] 一个 portal 组件，用于内嵌
  - [ ] 翻译面板，给出一个单词，或者一句话，然后就进行翻译
  - [ ] 包括各个翻译会用到的配置
  - [ ] 每个语言的 query 拥有独立的配置
  - [ ] 在外侧修改语言的配置
  - [ ] 每一个词典，都有对应的渲染 DOM


### stage 2

个性化，可以配置应用适合个人的配置

- [ ] 可以调用系统 OCR 进行识别
- [ ] 扩展性，应用可以扩展到更多的人去使用
- [ ] 添加划词检测，添加检测
- [ ] 外部系统同步功能
  - [ ] shanbay
  - [ ] anki
  - [ ] webdav


### stage 3

- [ ] 关注额外的平台，更多平台的支持

- 将应用内容扩展到我的另一个应用上

## 想要更简单的架构


