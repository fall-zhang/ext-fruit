export interface GoogleDictResult {
  entry: string
  styles: string[]
}

export interface FragmentInfo {
  id: string
  innerHTML: string
}

export interface ImageInfo {
  id: string
  src: string
}
