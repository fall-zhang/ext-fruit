import type { ApiInfo } from '../../types/api-info'

const guoyuConfig: ApiInfo = {
  type: 'self-trans',
  from: ['zh-TW'],
  to: ['zh-TW'],
  maxWord: 5,
  minWOrd: 1,
}

export default guoyuConfig
