import type { AtomFetchRequest, AtomResponseHandle } from '../types/atom-type'

// export const combine = (request: AtomFetchRequest, response: AtomResponseHandle) => {
//   const requestObj = request()
//   fetch(requestObj).then(res => response(res)).catch(err => {
//     console.log('⚡️ line:5 ~ err: ', err)
//   })
// }
